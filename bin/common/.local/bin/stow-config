#!/usr/bin/env bash

if [ "$(uname)" = "Linux" ]; then
  DIRS=(common linux)
elif [ "$(uname)" = "Darwin" ]; then
  DIRS=(common macos)
else
  DIRS=(common)
fi

stow "${DIRS[@]}" -d "$HOME/scripts/config" -t "$HOME" -v
stow "${DIRS[@]}" -d "$HOME/scripts/bin" -t "$HOME" -v
