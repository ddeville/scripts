" NOTE: This is disabled since most bzl code in rSERVER is not formatted.
"       Run `:FormatCode` manually to format bzl where needed.

" augroup autoformat_settings
"   autocmd FileType bzl AutoFormatBuffer buildifier
" augroup END
